#include <vector>
using namespace std;

vector<long long> shorten(int N, int M, int Q, vector<long long> K, vector<int> A, vector<int> B, vector<int> C) {
    vector<long long> ans(Q, 42);
    return ans;
}
